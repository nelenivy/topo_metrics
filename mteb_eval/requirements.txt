# Lean pip-only environment for `mteb_eval`.
#
# This file keeps only the packages imported directly by this repo (plus `mteb`)
# and pins them to the versions that work in the `embs_aggr` environment.
# A clean venv with this set was validated by:
#   - importing `scripts.run_unsup_eval`
#   - running `scripts/analyze_correlations.py --help`
#   - running `src.unsup_metrics.compute_metrics(...)`
#
# `mteb` pulls its own runtime deps such as `sentence-transformers`,
# `pytrec-eval-terrier`, `rich`, `pydantic`, `polars`, and `tqdm`.

mteb==2.7.22
datasets==4.5.0
transformers==5.0.0
torch==2.10.0
numpy==2.4.2
scipy==1.17.0
scikit-learn==1.8.0
scikit-dimension==0.3.4
pandas==3.0.0
h5py==3.15.1
cvxpy==1.8.0
ripser==0.6.14

# Optional extras:
# ripserplusplus==1.1.3  # faster persistence backend for larger Ripser runs
# torchph==0.1.1         # GPU PH-dim helper used only by src/topology.py
# hydra-core==1.3.2      # intrinsic_dim CLI wrappers under ../intrinsic_dim
# omegaconf==2.3.0       # intrinsic_dim CLI wrappers under ../intrinsic_dim
